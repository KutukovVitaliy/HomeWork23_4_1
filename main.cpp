#include <iostream>

#define WEEK_DAY(a)

#define DAY_1   "Monday"
int main() {
    int number;
    std::cout << "Input the number of the day of the week: ";
    std::cin >> number;

    WEEK_DAY(number);
    return 0;
}
